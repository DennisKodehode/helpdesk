# Leave empty for local dev — auth client falls back to window.location.origin
# and Vite's /api proxy handles the rest. Set this only when the API lives on
# a different origin in production.
VITE_API_URL=
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=
